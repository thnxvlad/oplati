services:
  postgres:
    image: postgres:16
    container_name: oplati-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: oplati
      POSTGRES_PASSWORD: oplati
      POSTGRES_DB: oplati
    ports:
      - "5432:5432"
    volumes:
      - oplati_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U oplati -d oplati"]
      interval: 5s
      timeout: 5s
      retries: 5
  redis:
    image: redis:7-alpine
    container_name: oplati-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  oplati_pg_data:
